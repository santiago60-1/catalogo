package com.tiquetera.catalog.config;

import org.springframework.boot.test.context.TestConfiguration;

@TestConfiguration
public class TestConfig {
    // Configuración de prueba adicional (por ejemplo, Beans de test utilities)
}